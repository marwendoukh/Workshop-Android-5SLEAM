<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tn.sleam.five.marwen.a5sleam">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WorkshopsList">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".recyclerview.ChooseCountry" />
        <activity android:name=".recyclerview.UsersList" />
        <activity android:name=".recyclerview.AddUser" />
        <activity android:name=".fragment.InitialActivityOfFragment" />
        <activity
            android:name=".sensors.SensorActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".sqlite.AddCandidate" />
        <activity android:name=".sqlite.RetreiveAllCandidates"></activity>
    </application>

</manifest>